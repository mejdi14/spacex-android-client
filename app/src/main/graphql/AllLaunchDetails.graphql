query AllLaunchDetails {
    launches {
        id
        site
        launch_year
        launch_date_utc
        mission {
            name
            missionPatch(size:LARGE)
        }
        links{article_link
            wikipedia
            youtube_id}
    }
}